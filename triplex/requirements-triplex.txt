# TRIPLEX hand-port deps (NO flash-attn, NO HEST, NO CLAM, NO MinkowskiEngine).
# torch/torchvision: install the CUDA build matching the pod, e.g.
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
einops
scipy
numpy
pandas
h5py
scanpy
tqdm
wget
