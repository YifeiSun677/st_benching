# BLEEP environment on a fresh RunPod pod.
# Only /workspace survives a pod teardown, so this gets reinstalled every time.
#
#   pip install -r bleep/requirements_bleep.txt
#
# torch/torchvision come with the RunPod PyTorch image -- do NOT reinstall them
# here or pip will pull a CPU build over the CUDA one.
#
# DO NOT add scprep. It pins pandas<2.1, which then builds from source and
# fails on Python 3.12. This has cost a rebuild before.

numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
anndata>=0.10
scanpy>=1.10
einops>=0.7
timm>=0.9
transformers>=4.40
Pillow>=10.0
tqdm>=4.66
matplotlib>=3.8
